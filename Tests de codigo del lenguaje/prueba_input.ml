void main() {
    int maximo;

    // Ahora le pasamos un cartel guía al usuario
    input("Ingrese la cantidad de vueltas: ", maximo);

    for (int i = 1; i <= maximo; i = i + 1) {
        print(i);
    }
}